name: Use Secrets
      your-secret:"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"
on:
  push:
    paths:
      - ".github/workflows/use-secrets.yml"

jobs:
  check-secret:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Grade use-secrets activity
        id: check-secrets
        uses: ./.github/actions/check-secrets-action
        with:
          secrets-context: ${{ toJSON(secrets) }}
